#include "../../../src/application/wordcount/summer.h"


void summerNotTestedNotice() {
	// This class should not be tested independently

	exit(0);
}

TEST(SummerTests, summerNotTestedNotice) {ASSERT_EXIT_ZERO(summerNotTestedNotice)}