#include "../../src/dataframe/fielder.h"


void fielderNotTestedNotice() {
	// This class cannot be tested independently.

	exit(0);
}

TEST(FielderTests, fielderNotTestedNotice) {ASSERT_EXIT_ZERO(fielderNotTestedNotice)}